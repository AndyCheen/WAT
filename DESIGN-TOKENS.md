# 🎨 Дизайн-токени та компоненти (витяг з макетів 1a / 2e / 3f / 4a)

> Джерело: `WaterTracker.html` (розпакований шаблон design-canvas).
> Кадр пристрою в макетах — **402 × 874** (iPhone 16 Pro, логічні пункти).
> Тому **1 CSS-піксель = 1 pt у SwiftUI** — усі розміри переносяться 1:1 без перерахунку.

---

## 1. Типографіка

Дві гарнітури Google Fonts (ліцензія OFL, бандляться в застосунок — `App/Resources/Fonts`).

⚠️ **Ключова деталь.** У самому макеті Fredoka підключена **без кириличних підмножин** —
`unicode-range` кирилиці є тільки в Nunito. Тобто весь український текст у макеті де-факто
рендериться Nunito, а Fredoka дістається лише цифрам і латиниці. Тому в застосунку:

| Роль (`WTFont`) | Гарнітура | Де вживається |
|---|---|---|
| `display(size, weight)` | **Nunito** Bold / SemiBold | заголовки, назви карток, кнопки, рядки списків |
| `text(size, weight)` | **Nunito** 500–900 | підписи, легенди, службові лейбли, прогрес |
| `number(size, weight)` | **Fredoka** SemiBold / Bold | суто числові акценти: % на кільці, номер рівня, значення в шторці, лічильник «5/7» |

Якщо гарнітури з бандла зникнуть, `WTFont` мовчки повертається до системного rounded —
макет не ламається.

Шкала (усі значення зустрічаються в макетах):

| Токен | Розмір / вага | Де вживається |
|---|---|---|
| `number` | 64 / 600 | % на кільці (1a) |
| `number` | 52 / 600 | значення в шторці «Скільки ви випили?» |
| `number` | 32 / 700 | номер рівня в донаті (3f) |
| `display` | 30 / 700 | великі числові підсумки |
| `display` | 22 / 600 | заголовки шторок |
| `display` | 20 / 600 | заголовок екрана (2e, 4a) |
| `display` | 16 / 600 | заголовки карток-графіків |
| `display` | 15–18 / 600 | рядки завдань, історії, призів |
| `text` | 13 / 800, tracking .3 | «ЗАВДАННЯ», «ПРИЗИ», «ВАГА» |
| `text` | 15 / 800, tracking .3 | «ЗАВДАННЯ НА СЬОГОДНІ», «ІСТОРІЯ» (1a) |
| `text` | 12–14 / 700–800 | підписи осей, прогрес, легенди |
| `text` | 10–11 / 700–800 | підписи теплокарти, «РІВЕНЬ» |

---

## 2. Кольори

### 2.1 Семантичні токени теми (з `view()` макета 1a — єдине місце, де є обидві теми)

| Токен | Light | Dark |
|---|---|---|
| `bg.screen` | `#f7fbff` | `#0c1524` |
| `bg.card` | `#ffffff` | `#161f33` |
| `bg.sheet` | `#ffffff` | `#131e30` |
| `bg.chip` | `#eaf4ff` | `#17233a` |
| `bg.button` | `#eaf4ff` | `#17233a` |
| `text.primary` | `#1f4a72` | `#eaf2ff` |
| `text.muted` | `#8aa1bd` | `#8ba0bd` |
| `text.button` | `#3d8fd6` | `#bcd8ff` |
| `accent` | `#5aa9f0` | `#7cc4ff` |
| `line` | `rgba(31,74,114,0.10)` | `rgba(255,255,255,0.08)` |
| `track` | `#e4f0fb` | `rgba(255,255,255,0.08)` |
| `dot.off` | `#dce8f3` | `rgba(255,255,255,0.14)` |
| `delete.bg` | `#fdecec` | `rgba(229,72,77,0.16)` |
| градієнт кільця | `#bfe3ff → #6bb6f2` | `#8fd0ff → #3b6ef5` |

⚠️ Макети 2e, 3f, 4a намальовані **лише у світлій темі**. Темні значення для них виводимо через ті самі токени (пряма підстановка з таблиці вище) — це окремий пункт дизайн-приймання (див. PLAN.md, Фаза 8).

### 2.2 Фіксовані акценти (не залежать від теми)

| Токен | Значення | Де |
|---|---|---|
| `accent.orange` | `#ff8a3d` | стрік-крапля, кнопка «Активувати», бейдж нового досягнення |
| `success` | `#3fb56f` (та `#3ea86b` для тексту) | галочка «ціль виконана», статус «Виконано» |
| `danger` | `#e5484d` | кнопка «Видалити» |
| `warn.bg` / `warn.fg` | `#fdf1da` / `#a97a1f` | бейдж «47 / 100» рівномірності |
| `prize.bg` / `prize.icon` | `#fff8ec` / `#ffe9c8` | картки призів (3f) |
| `gold.iconBg` | `#fff2e2` | іконки відкритих досягнень / нагород рівня |
| `text.secondary` | `#3d4d66`, `#5c7291` | підписи рядків графіків, текст модалок |
| `neutral.*` | `#eef2f7`, `#e4e8ee`, `#f2f4f7`, `#e6e8eb`, `#b7c3d4`, `#d7dee8`, `#c3cddb` | треки графіків, заблоковані стани |

### 2.3 Шкали даних

- **Календар-теплокарта (4a):** `rgba(90,169,240, 0.25 + p·0.75)`, де `p = min(pct,100)/100`; `pct = 0` → `#e4e8ee`; майбутні дні → `#f2f4f7` / текст `#c3cddb`.
  Текст усередині кружка білий, якщо `p > 0.45`, інакше `#5c7291`. Сьогодні — рамка `2px #1f4a72`; вибраний день — `box-shadow 0 0 0 3px rgba(31,74,114,.45)`.
- **Теплокарта «Коли ти п'єш» (4a):** 5 рівнів — `#eef2f7`, `rgba(90,169,240,.28)`, `.52`, `.76`, `1`.
- **Легенда норми:** горизонтальний градієнт `#e4e8ee → rgba(90,169,240,1)`.

---

## 3. Геометрія

| Параметр | Значення |
|---|---|
| Радіуси | 4, 8, 9, 10, 11, 12, 14, 18, 20, 22, 24, 32 (верх шторки), 999 (pill) |
| Падінги екрана | top 64, bottom 44, horizontal **26** (1a) / **24** (2e, 3f, 4a) |
| Картка-графік | `radius 24`, padding `20 / 18`, `margin-bottom 18` |
| Тінь картки | `0 8px 24px rgba(30,60,120,.06)` |
| Тінь шторки | `0 -12px 40px rgba(8,16,32,.15)` |
| Тінь модалки | `0 20px 50px rgba(20,40,80,.25)` (2e) / `0 24px 60px rgba(20,40,70,.3)` (4a) |
| Ручка шторки | 44 × 5, radius 99, колір `track`, `margin 0 auto 18–20` |
| Кільце прогресу (1a) | контейнер 242 × 242, viewBox 200, r = 88, товщина 17, `linecap: round`, старт з -90° |
| Донат рівня (3f) | 112 × 112, внутрішній інсет 8, conic-gradient від accent до `#e4f0fb` |
| Кільце прогресу досягнень (2e) | 62 × 62, r = 26, товщина 7 |
| Тогл | 52 × 31, кружок 25, зсув 3 → 24 pt |

## 4. Анімації

| Назва | Параметри |
|---|---|
| `sheetUp` | `translateY(100% → 0)`, 0.32 s, `cubic-bezier(.22,1,.36,1)` → `.spring(response: 0.42, dampingFraction: 0.86)` |
| `fadeIn` (димка) | opacity 0 → 1, 0.2 s ease |
| заповнення кільця | `stroke-dashoffset` 0.75 s `cubic-bezier(.22,1,.36,1)` |
| тогл, ширина XP-бару | 0.2 s / 0.5 s ease |

---

## 5. Інвентар компонентів дизайн-системи

Кожен компонент — окремий файл у пакеті `DesignSystem`, з `#Preview` у світлій і темній темі.

**Примітиви**
- `WTText` (обгортка стилів типографіки), `WTIcon` (SF Symbols / кастомні шляхи SVG)
- `WTCard` (радіус 24 + тінь), `WTChipButton`, `WTPrimaryButton`, `WTCircleButton` (38 / 42 / 26 pt)
- `WTToggle`, `WTSegmentedTabs` (pill-таби: `chartModes`, `statsPeriodOpts`, категорії 2e)
- `WTSheet` (ручка + радіус 32 + тінь + анімація), `WTModal` (2e/4a)
- `WTNavBar` (кнопка «назад» + заголовок + баланс справа)

**Індикатори**
- `ProgressRing` (кільце 1a з градієнтом), `LevelDonut` (3f), `MiniProgressRing` (2e)
- `XPBar`, `ProgressBarSmall` (плитки досягнень 2e)

**Списки та рядки**
- `TaskRow` (кружок-галочка + назва + прогрес) — спільний для 1a і 3f
- `HistoryTimelineRow` (крапка + лінія + мл + час, стан «відкрито → Видалити») — 1a
- `HistoryStaticRow` — 4a (без видалення)
- `PrizeCard`, `LevelRewardRow`, `RewardPreviewRow`
- `AchievementTile` (сітка 3×N з прогрес-баром — 2e), `AchievementCard` (сітка 2×N — шторки 1a/3f), `AchievementBadgeSmall` (сітка 4×N — 3f)

**Графіки** (усе на нативному SwiftUI `Canvas`/`Shape`, без Swift Charts — щоб точно тримати макет)
- `BarChartWithGoalLine` (4a «Об'єм по днях» + пунктир цілі), `SimpleBarChart` (шторка статистики 1a, ритм дня)
- `EvennessRow` (смуга «випито» + ризка «ціль»), `MedianSpreadRow` (медіана + діапазон + ризка)
- `CalendarHeatmap` (місяць, галочка «ціль виконана», свайп між місяцями), `WeekHourHeatmap` (7 × 9)
- `LegendRow` (набір міток легенд)

---

## 6. Мапінг CSS → SwiftUI

| CSS | SwiftUI |
|---|---|
| `border-radius` | `.clipShape(RoundedRectangle(cornerRadius:style: .continuous))` |
| `box-shadow: 0 8px 24px rgba(...)` | `.shadow(color:radius: 12, x: 0, y: 8)` (radius = blur/2) |
| `letter-spacing: .3px` | `.tracking(0.3)` |
| `conic-gradient` | `AngularGradient` |
| `stroke-dasharray/offset` | `Circle().trim(from:to:).stroke(style: StrokeStyle(lineWidth: 17, lineCap: .round))` |
| `aspect-ratio: 1` | `.aspectRatio(1, contentMode: .fit)` |
| `sc-for` / `sc-if` | `ForEach` / `if` |
| `position: absolute; inset: 0` | `ZStack` + `.frame(maxWidth:maxHeight:)` |
| нижня шторка | `.presentationDetents` або власний `WTSheet` в `ZStack` (обираємо власний — макет має нестандартний радіус і тінь) |
